---
title: "Projects"
listing: 
  - id: projects
    contents: projects
    type: grid
    fields: ['image', 'title', 'date', 'categories']
    sort: "date desc"
    sort-ui: [date, title]
    grid-columns: 3
    categories: true
    filter-ui: true
---

Here are some of my projects that I have worked on - spanning from architecture and design-oriented projects to ones heavy on spatial analytics.

## Featured Projects

<div id="carousel" class="carousel">
<div class="carousel-slides-wrapper">
<div class="carousel-slide active">
<a href="projects/cycle_shortcut_map.html">
<div class="carousel-image-wrapper">
<img src="https://nishikata-tokotoko.github.io/cycle-shortcut-map/img/ogp_index.png" alt="Cycle-Shortcut: Beat the Winding Rails">
</div>
<div class="carousel-caption">Cycle-Shortcut: Beat the Winding Rails</div>
</a>
</div>
<div class="carousel-slide">
<a href="projects/GISCI_map_contest_2024.html">
<div class="carousel-image-wrapper">
<img src="projects/img/fuji36_map.png" alt="Map of the &quot;Thirty-six Views of Mount Fuji&quot;">
</div>
<div class="carousel-caption">Map of the "Thirty-six Views of Mount Fuji</div>
</a>
</div>
<div class="carousel-slide">
<a href="projects/just_energy.html">
<div class="carousel-image-wrapper">
<img src="projects/img/just_energy/key_image_final.png" alt="Catalysing an Energy-Driven Transformation">
</div>
<div class="carousel-caption">Catalysing an Energy-Driven Transformation</div>
</a>
</div>
</div>
<button class="carousel-btn prev" onclick="plusSlides(-1)">&#10094;</button>
<button class="carousel-btn next" onclick="plusSlides(1)">&#10095;</button>
</div>

## Browse More Projects

:::{#projects}
:::

::: {.cell}
```{=html}
<script>
let slideIndex = 0;
function showSlides(n) {
  const slides = document.querySelectorAll('.carousel-slide');
  const wrapper = document.querySelector('.carousel-slides-wrapper');
  if (!slides.length || !wrapper) return;
  slideIndex = (n + slides.length) % slides.length;
  wrapper.style.transform = `translateX(-${slideIndex * 100}%)`;
  slides.forEach((slide, i) => {
    slide.classList.toggle('active', i === slideIndex);
  });
}
function plusSlides(n) {
  showSlides(slideIndex + n);
  resetAutoScroll();
}
function autoScroll() {
  plusSlides(1);
}
function resetAutoScroll() {
  if (autoScrollInterval) clearInterval(autoScrollInterval);
  autoScrollInterval = setInterval(autoScroll, 4000); // 4 seconds per slide
}
document.addEventListener('DOMContentLoaded', () => {
  showSlides(slideIndex);
  resetAutoScroll();
});
</script>
```
:::